// export default ({ store, redirect }) => {
export default ctx => {
  console.log('unAuthRedirect')
  console.log(ctx)
  // console.log(store)
  // console.log(redirect)
  // if (!store.state.authUser) {
  //   return redirect('/top')
  // }
}
